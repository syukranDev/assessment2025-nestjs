
revision nestjs 30sept2025 
tutorial reference: https://www.youtube.com/watch?v=2gtiffE3__U

npm i -g @nestjs/cli

--creat new project
nest new assesment2025-nestjs

// nestjs flow normally
1) add module
2) add controller
3) add api logic via service

-- crewte new modules via cli
-- each module will have it ows decorators (without controller)
nest generate  module <name it anything>
nest generate module posts
nest generate module tags
nest generate module users

=========================================================
CONTROLLER 
=========================================================

--create new controller via cli
--buat sendiri pun ok if remember boilerplate 
nest generate controller <name of the module>

note: 
-inside controller use dependency injection via constructor
-use @Injectable in service, as below
extra: 
-PATTERN: can inject service from other module as well, then set in constructor
to call it 

=========================================================
PROVIDER (service) - its for API logic
=========================================================

-- create new service
nest generate service <name follow module>

=========================================================
ERROR HANDLING
=========================================================
return new HttpException('data not found', 422)  -- boleh guna custom status code
return new NotFoundException('data not found')  -- fix 404

=========================================================
Pipes (for data processing from client)
=========================================================
-validate data at AFTER routing, before controller
-NOTE: not same as Middleware expressjs, BEFORE routing


i.e, ParseIntPipe, DefaultValuePipe etc see docs
OR create custom pipes folder as injectable, (use PipeTransfrom)

=========================================================
class validator for typescript DTO &&  ValidationPipe 
=========================================================
pros: reject invalid data with 400 error
-validate our decorator
- without ValidationPipe, decorator we set will be useless


=========================================================
Guards (like expressJS auth middleware)
=========================================================
apply to all  --> on top of controller
apply to endpoint --> on top of @Get, @Post


npm run db:generate -- src/database/migrations/<name of the migration can be anthing>
npm run db:run
see package.json for script details




=========================================================
etc, findings 
=========================================================
-use forwrdRef if module A uses module B and module B use module B, 
 to avoid circular dependency (thats new)

-use JwtModule.registerASync to ensure .env finish uploaded, 
 without this will be undefined at auth.module during runtimee


- CQRS - seperate write & read operation
* npm install @nestjs/cqrs
* command handler - for WRITE operation
* query handler - for READ operation

* IMPROTANT: in controller, the commandBus payload must match ordering likw what stated in command file
* IMPORTANT: commands must follow DTO i.e optional etc


TODO learn implementation:
GraphQL -



